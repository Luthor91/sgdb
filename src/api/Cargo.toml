[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.9.0"  # Ou toute version que tu utilises pour Actix
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
models = { path = "../models" }

